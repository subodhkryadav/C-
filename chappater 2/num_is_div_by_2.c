#include<stdio.h>
int main(){
int num;
printf("enter the number ");
scanf("%d",&num);
if(num%2==0)
  printf("number is divisible by 2");
else 
  printf("number is not divisible by 2");
    return 0;
}